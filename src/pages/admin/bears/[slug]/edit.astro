---
export const prerender = false;
import AdminLayout from '@/layouts/admin.layout.astro';
import AdminForm from '@/components/admin/AdminForm';
import { bearFields } from '@/lib/admin/field-defs';
import { getEntry } from '@/lib/admin/content-manager';

if (!import.meta.env.DEV) {
  return Astro.redirect('/404');
}

const { slug } = Astro.params;
const entry = await getEntry('bears', slug!);

if (!entry) {
  return Astro.redirect('/admin/bears');
}
---

<AdminLayout title={`Edit ${entry.frontmatter.name}`}>
  <h1>Edit Bear: {entry.frontmatter.name}</h1>
  <AdminForm
    client:load
    collection="bears"
    fields={bearFields}
    initialData={entry.frontmatter}
    initialBody={entry.body}
    initialSlug={slug}
    isEdit={true}
    enableAI={true}
  />
</AdminLayout>
