---
import { PawPrint } from "lucide-react";
import Block from "../ui/block.astro";
import Container from "../ui/container.astro";
import H3 from "../ui/headings/h3.astro";

interface Props {
    buttons: string[][];
}

const { buttons }: Props = Astro.props;
---

<Container>
    <Block>
        <div class="flex flex-col md:flex-row gap-4 md:gap-8">
            {
                buttons.map(([url, label]) => (
                    <a
                        href={url}
                        class="group relative overflow-hidden flex order-1 bg-mandy-100 hover:bg-mandy-200 p-4 flex-1 text-center justify-center items-center py-10 md:py-20 transition-colors delay-75 rounded"
                    >
                        <PawPrint className="absolute fill-mandy-50 stroke-mandy-50 z-0 -top-10 right-0 size-64 opacity-60" />
                        <H3 className="z-10"> {label} </H3>
                    </a>
                ))
            }
        </div>
    </Block>
</Container>
