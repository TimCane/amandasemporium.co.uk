---
// Component spec: Task 1.5 shared UI — Filter Tabs
interface Tab {
  label: string;
  value: string;
  href: string;
}

interface Props {
  tabs: Tab[];
  activeValue: string;
}

const { tabs, activeValue } = Astro.props;
---

<nav class="filter-tabs" aria-label="Filter bears">
  {tabs.map(tab => (
    <a
      href={tab.href}
      class:list={['filter-tabs__tab', { 'filter-tabs__tab--active': tab.value === activeValue }]}
      aria-current={tab.value === activeValue ? 'page' : undefined}
    >
      {tab.label}
    </a>
  ))}
</nav>
